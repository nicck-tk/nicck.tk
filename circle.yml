machine:
  hosts:
    nck-nano: 130.255.188.231
dependencies:
  post:
    - npm install roco
deployment:
  production:
    branch: master
    commands:
      - printf "\nHost nck-nano\n  Port 2600\n  User root" >> ~/.ssh/config
      - ./node_modules/.bin/roco deploy
